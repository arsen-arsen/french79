#include <stdio.h>
int main () {

printf ("Hello, world!\n")

	return 0
}
float function(float x, float z)
{
  float y;
  y=x+z;
  return(y);
  printf (y)
}
 float function(float x, float z)
{
  float y;
  y=x*z;
  return(y);
  printf (y)
} 
 float function(float x, float z)
{
  float y;
  y=x-z;
  return(y);
  printf (y)
} 